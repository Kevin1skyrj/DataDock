import { SecuritySettings } from "@/app/dashboard/settings/security/security-settings";

export const metadata = { title: "Security" };

export default function SecurityPage() {
  return <SecuritySettings />;
}
