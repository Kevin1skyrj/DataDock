import { ShortcutsSettings } from "@/app/dashboard/settings/shortcuts/shortcuts-settings";

export const metadata = { title: "Keyboard shortcuts" };

export default function Page() {
  return <ShortcutsSettings />;
}
