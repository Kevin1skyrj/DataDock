import { AppearanceSettings } from "@/app/dashboard/settings/appearance/appearance-settings";

export const metadata = { title: "Appearance" };

export default function Page() {
  return <AppearanceSettings />;
}
