import { NotificationSettings } from "@/app/dashboard/settings/notifications/notification-settings";

export const metadata = { title: "Notifications" };

export default function Page() {
  return <NotificationSettings />;
}
