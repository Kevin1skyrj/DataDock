import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export const metadata = { title: "Starred" };

export default function Page() {
  return (
    <PagePlaceholder
      title="Starred"
      description="The files you have marked to keep close."
    />
  );
}
