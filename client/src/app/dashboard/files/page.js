import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export const metadata = { title: "All files" };

export default function Page() {
  return (
    <PagePlaceholder
      title="Files Browser"
      description="The full drive, with folder navigation, sorting and views."
    />
  );
}
